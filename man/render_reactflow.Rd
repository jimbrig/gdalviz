% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render-reactflow.R
\name{render_reactflow}
\alias{render_reactflow}
\title{Render a pipeline graph as an interactive React Flow widget}
\usage{
render_reactflow(
  graph,
  direction = c("TB", "LR", "BT", "RL"),
  theme = c("light", "dark"),
  minimap = TRUE,
  controls = TRUE,
  legend = TRUE,
  draggable = TRUE,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{graph}{A \code{gdalviz_graph} from \code{\link[=pipeline_graph]{pipeline_graph()}}, or a
pipeline/string/path accepted by it.}

\item{direction}{Layout direction: \code{"TB"} (default), \code{"LR"}, \code{"BT"}, \code{"RL"}.}

\item{theme}{\code{"light"} (default) or \code{"dark"}.}

\item{minimap}{Show a minimap overview. Defaults to \code{TRUE}.}

\item{controls}{Show zoom/fit controls. Defaults to \code{TRUE}.}

\item{legend}{Show the category legend. Defaults to \code{TRUE}.}

\item{draggable}{Allow nodes to be repositioned by dragging. Defaults to
\code{TRUE}.}

\item{width, height}{Widget dimensions passed to
\code{\link[htmlwidgets:createWidget]{htmlwidgets::createWidget()}}.}

\item{elementId}{Optional explicit element id for the widget container.}
}
\value{
A \code{pipeline_flow} htmlwidget.
}
\description{
Produces a modern, interactive dataflow diagram of a GDAL pipeline using
\href{https://reactflow.dev}{React Flow} (xyflow). Steps render as card-style
nodes colored by category with their arguments inline; edges carry
state-change badges (CRS, geometry type, field count); tee branches render
as dashed side flows. Clicking a node opens an inspector panel with the
full argument list, the propagated stream state, and a link to the GDAL
documentation for that step.
}
\details{
The JavaScript bundle is built from \verb{srcjs/} (see
\code{srcjs/README.md}) and shipped with the package, so no node toolchain is
needed at run time.
}
\examples{
\dontrun{
gdalg <- system.file(
  "extdata", "pipelines", "tiger_states.gdalg.json",
  package = "gdalviz"
)
pipeline_graph(gdalg) |> render_reactflow(theme = "dark")
}

}
