% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{render_script}
\alias{render_script}
\title{Render a pipeline as a formatted shell script}
\usage{
render_script(
  x,
  shell = c("bash", "powershell"),
  indent = "  ",
  type = NULL,
  globals_per_line = 3L,
  sql = c("inline", "block")
)
}
\arguments{
\item{x}{A \code{gdalviz_pipeline}, or a string/path accepted by \code{\link[=read_gdalg]{read_gdalg()}}.}

\item{shell}{Target shell: \code{"bash"} (default) or \code{"powershell"}.}

\item{indent}{Indentation unit for steps. Defaults to two spaces.}

\item{type}{Pipeline type (\code{"vector"} or \code{"raster"}).}

\item{globals_per_line}{Number of global options to place per continuation
line. Defaults to \code{3}.}

\item{sql}{Either \code{"inline"} (default, single quoted token) or \code{"block"}
(multiline heredoc / here-string for \code{--sql} values).}
}
\value{
A length-one character vector containing the full script.
}
\description{
Produces an indented, line-continued \verb{gdal <type> pipeline} invocation for
either \code{bash}/\code{sh} or \code{powershell}, with one \code{! step} per line and
shell-appropriate quoting. Optionally reflows large SQL into a heredoc
(bash) or here-string (powershell) for readability.
}
