% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contract.R
\name{gdalviz_refresh_contract}
\alias{gdalviz_refresh_contract}
\title{Regenerate the GDAL pipeline contract snapshot from the installed GDAL}
\usage{
gdalviz_refresh_contract(
  path = NULL,
  type = c("vector", "raster"),
  gdal = NULL
)
}
\arguments{
\item{path}{Destination for the snapshot JSON. Defaults to the bundled
snapshot location, replacing it for the current installation.}

\item{type}{Pipeline type (\code{"vector"} or \code{"raster"}).}

\item{gdal}{Path to the \code{gdal} binary. Defaults to \code{gdal} on the \code{PATH}.}
}
\value{
The refreshed \code{gdalviz_contract}, invisibly.
}
\description{
Runs \verb{gdal <type> pipeline --json-usage} against the GDAL CLI on the user's
\code{PATH} (or an explicit binary) and writes the result as a new contract
snapshot, so the contract registry matches the locally installed GDAL
version instead of the snapshot bundled with the package.
}
