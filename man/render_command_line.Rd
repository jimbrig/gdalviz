% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{render_command_line}
\alias{render_command_line}
\title{Render a pipeline as a canonical GDALG command line}
\usage{
render_command_line(x, prog = TRUE, type = NULL)
}
\arguments{
\item{x}{A \code{gdalviz_pipeline}, or a string/path accepted by \code{\link[=read_gdalg]{read_gdalg()}}.}

\item{prog}{Logical or \code{NULL}. Whether to emit the leading
\verb{gdal <type> pipeline} program prefix. Defaults to \code{TRUE}.}

\item{type}{Pipeline type (\code{"vector"} or \code{"raster"}). Defaults to the type
detected during parsing, falling back to \code{"vector"}.}
}
\value{
A length-one character vector.
}
\description{
Serializes a parsed \code{\link[=parse_pipeline]{parse_pipeline()}} result back into a single-line
\code{command_line} string, using the same minimal quoting that GDAL itself uses
when it writes \verb{*.gdalg.json} files. The result round-trips: parsing the
output again yields an equivalent pipeline.
}
